# Install the following packages:

```sh
sudo apt-get install \
	--no-install-recommends \
	build-essential \
	freeglut3-dev \
	libglm-dev \
	libxmu-dev \
	libxi-dev \
	mesa-utils \

```

# Generate makefile by cmake

```sh
cmake .

```

# Compile:

```sh
make

```

It's all!

