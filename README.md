# Install the following packages:

```sh
sudo apt-get install \
	--no-install-recommends \
	build-essential \
	freeglut3-dev \
	libglm-dev \
	mesa-utils
	# libxmu-dev
	# libxi-dev

```

# Generate makefile by cmake

```sh
cmake .
```

# Compile:

```sh
make
```

It's all!

